# 添加第三方库的源文件
# file(GLOB SQLPARSER_SOURCES "./sql-parser/src/*.cpp")

# add_library(third_party ${SQLPARSER_SOURCES})

# 设置包含路径，如果有的话
# target_include_directories(third_party PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/sql-parser/src)

